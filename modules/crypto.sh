#!/usr/bin/env bash

# BOOTSTRAP
	source "$(dirname "${BASH_SOURCE[0]}")/../libs/bootstrap.sh"
	load_libs animations
	set_title "🪙  CRYPTO 🪙"
	required_commands

echo -e " this is a work in progress\n"
press_any_key
